# Phase 3: The Flow Canvas (Svelte Flow)

1.  **Rendering:** Implement `<SvelteFlow />` component.
2.  **Binding:** Bind `nodes` and `edges` using `$state.raw` for performance.
3.  **Event Handling:** `onNodeDragStop` updates `AppState.positions`.
    *   Triggers URL sync automatically.
